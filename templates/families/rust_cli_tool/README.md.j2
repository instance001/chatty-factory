# {{ title }}

{{ summary }}

## Run

Run:

{% if tool_kind == "file_sorter" %}
`cargo run -- --input fixtures/input --output fixtures/output`

Expected result:

`fixtures/output/txt/sample.txt` should exist after the run.
{% elif tool_kind == "csv_report" %}
`cargo run -- --input fixtures/input/sample.csv`

Expected output shape:

`rows=<n> columns=<n> header=<comma-separated-columns>`
{% elif tool_kind == "log_summary" %}
`cargo run -- --input fixtures/input/app.log`

Expected output shape:

`errors=<n> warnings=<n> infos=<n>`
{% elif tool_kind == "text_stats" %}
`cargo run -- --input fixtures/input/sample.txt`

Expected output shape:

`lines=<n> words=<n> chars=<n>`
{% else %}
`cargo run -- --input fixtures/input`

Expected output shape:

`files=<n> ext:<extension>=<count> ...`
{% endif %}
