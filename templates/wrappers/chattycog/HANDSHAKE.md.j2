# {{ display_name }}

This module was built by the ChattyFactory rebuild as a ChattyCog-compatible dashboard module.

## Module identity

- **module_id**: `{{ module_id }}`
- **display_name**: `{{ display_name }}`

## What this module is for

{{ description }}

## Inputs this module expects

- plain-language dashboard or module requests
- follow-up feature and patch requests
- optional hosted ChattyCog bridge state when the module is docked inside ChattyCog

## Outputs this module produces

- a standalone web dashboard
- ChattyCog module wrapper metadata
- a portable bridge status lane
- deterministic project and acceptance contracts

## Module Surface

- standalone web dashboard
- ChattyCog wrapper metadata
- bridge stub
- project contract and acceptance lane

## Suspend rundown template

> **Status:** module scaffold is live and ready for feature work
> **What changed:** deterministic wrapper files and dashboard assets were emitted
> **Open questions:** confirm the next module capability or patch lane to add
> **Next action:** apply the next accepted deterministic family or patch route
> **Artifacts:** `ProjectSpec.json`, `AcceptancePlan.json`, `bridge/status.json`

## Notes

{{ handshake_notes }}
