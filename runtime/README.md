# Runtime

This folder is reserved for local rebuild runtime state.

Planned contents include:
- route receipts
- acceptance receipts
- failure reports
- transient build artifacts
- local execution logs

This is the rebuild-local equivalent of "runtime machinery lives here, not mixed into docs or source."
