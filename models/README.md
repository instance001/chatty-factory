# Models

Drop local GGUFs and related model files here when the rebuild grows its local-model runtime back in.

Purpose:
- make model storage obvious at a glance
- keep runtime/model assets separate from source and docs
- avoid path archaeology later
